{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start:dev": "lerna run start:dev --parallel",
    "start:up:prod": "docker-compose -f docker-compose.yml up -d --build",
    "start:down:prod": "docker-compose -f docker-compose.yml down",
    "up:dev": "docker-compose -f docker-compose.dev.yml up -d",
    "down:dev": "docker-compose -f docker-compose.dev.yml down"
  }
}
